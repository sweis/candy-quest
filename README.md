# candy-quest
Candy Quest — https://candyquest.live

The title screen shows a build stamp (`window.CQ_BUILD` in `index.html`). A pre-commit hook in `.githooks/` bumps it automatically; enable it once per clone:

    git config core.hooksPath .githooks
